---

---
<a target="_blank" href="https://marketplace.visualstudio.com/items?itemName=haderman.alanai">
  Try out Alan Chat
</a>


<style>
  a {
    font-family: system-ui, sans-serif;
    text-shadow: 1px 1px 0px black;
    text-decoration: none;
    position: relative;
    z-index: 2;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 8px 16px;
    color: hsl(210 40% 80%);
    border-radius: 4px;
    background: hsl(210 0% 0% / 0.5);
    border: 0.1px solid hsl(211 81% 73%);
    transition: border-color background-color 0.2s ease-in-out;
    box-shadow: inset 0px 0px 2px 1px hsl(218deg 67% 58% / 64%),
      0px 0px 5px 0.5px hsl(218deg 67% 58% / 64%);
  }

  a::after {
    content: "";
    position: absolute;
    width: 50%;
    height: 50%;
    top: 50%;
    left: 0;
    transform: translateY(-50%);
    background-color: hsl(256, 66%, 48%);
    filter: blur(15px);
    z-index: -1;
    /* animation: shine 4s linear infinite; */
  }

  a::before {
    content: "";
    position: absolute;
    width: 50%;
    height: 50%;
    top: 50%;
    right: 0;
    transform: translateY(-50%);
    background-color: hsl(210 95% 59% / 1);
    z-index: -1;
    filter: blur(13px);
    /* animation: shine 4s linear infinite; */
  }

  a:hover {
    border-color: hsl(211 81% 93%);
    background-color: black;
  }

  /* a:hover::after {
    background-color: hsl(256, 66%, 48%);
    filter: blur(45px);
  }

  a:hover::before {
    background-color: hsl(210 60% 46% / 1);
  } */

  /* a:focus::after {
    animation: shine 2s linear infinite;
  }

  a:focus::before {
    animation: shine 2s linear infinite;
  } */

  @keyframes shine {
  0% {
    transform: translateX(-100%);
    border: 0.5px solid hsl(210 40% 20%);
  }
  50% {
    transform: translateX(100%);
    border: 1px solid hsl(210 40% 40%);
  }
  100% {
    transform: translateX(-100%);
    border: 0.5px solid hsl(210 40% 20%);
  }
}

</style>
