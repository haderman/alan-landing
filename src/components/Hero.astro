---
import CTA from "./common/CTA.astro";
import Video from "./hero/Video.astro";
---

<section class="hero">
  <article>
    <h1 class="text-gradient">
      Multiple AI Chats Right in <br />Your IDE!
    </h1>
    <h2>Set up multiple AI chats. Each one has its own system prompt.</h2>
    <CTA />
  </article>
  <Video />
</section>

<style is:global lang="scss">
  .hero {
    display: flex;
    flex-direction: column;
    gap: var(--fluid-8);
    align-items: center;
    flex-wrap: wrap;
    height: 100%;
    position: relative;
    & .wrapper-hero-video {
      z-index: 1;
      flex-grow: 1;
      flex-shrink: 1;
      width: 100%;
    }
  }
  .hero > article {
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    flex-shrink: 1;
    flex-grow: 1;
    margin: auto;
    & > h1 {
      margin-bottom: var(--fluid-1);
      text-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
    }
    & > h2 {
      margin-bottom: var(--fluid-4);
      text-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
    }
  }
</style>
